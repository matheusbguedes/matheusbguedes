name: Generate Datas

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    name: Jobs
    steps:
      - uses: actions/checkout@v2
      - uses: Platane/snk@master
        with:
          github_user_name: borgeta-code
          svg_out_path: dist/github-contribution-grid-snake.svg
